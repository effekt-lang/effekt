name: Main CI

on:
  push:
    branches:
      - main

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
    with:
      # The change already passed tests on its PR; retrying keeps flaky tests from turning `main` red
      max-attempts: 3
