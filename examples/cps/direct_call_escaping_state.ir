def main(value, ks, k) {
  var cell = value @ ks;
  k(cell, ks)
}

// Returning the reference makes its association with the current
// meta-continuation observable, so the CPS convention must be retained.
/// CALLING_CONVENTIONS

-

/// JAVASCRIPT

function main_0(value_0, ks_0, k_0) {
  const cell_0 = VAR(value_0, ks_0);
  return k_0(cell_0, ks_0);
}
