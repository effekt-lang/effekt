def main(f, x) {
  f(x)
}

// Unknown callees use the stack-safe value-level calling convention.
/// CONTROL_FLOW

ranks
  main = 0
transfers
  main -> f: safe [?]

/// SAFE_ENTRIES

-

/// JAVASCRIPT

function main_0(f_0, x_0) {
  return f_0(x_0);
}
