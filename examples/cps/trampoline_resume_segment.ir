def main(r0, ks0, k0) {
  resume(r0) { ks, k =>
    k(1, ks)
  } @ ks0, k0
}

// Resuming enters the captured continuation segment through the trampoline.
/// JAVASCRIPT

function main_0(r_0, ks_0, k_0) {
  {
    const [ks_1, k_1] = RESUME(r_0, ks_0, k_0);
    return () => k_1(1, ks_1);
  }
}
