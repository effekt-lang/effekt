def main(p0, ks0, k0) {
  shift(p0) { r, ks, k =>
    k(1, ks)
  } @ ks0, k0
}

// The resumption is unused, so this shift is an abortive transfer. The current
// translation nevertheless captures and snapshots the segment with SHIFT;
// calling the handler continuation then crosses the trampoline once.
/// JAVASCRIPT

function main_0(p_0, ks_0, k_0) {
  {
    const [r_0, ks_1, k_1] = SHIFT(p_0, ks_0, k_0);
    return () => k_1(1, ks_1);
  }
}
