type OptionInt {
  None();
  Some(head: Int)
}

def main() = {
  val y:Int = 3;
  val x:OptionInt = Some(2);
  val z:Int = 5;
  println(y);
  x match {
  case None() => println(0);
  case Some(h) => println(h);
  }
  println(z);
}
