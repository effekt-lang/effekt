
effect E() : (() => Unit at {io})


def main() : Unit = {
    try {
      val f = do E()
      f()
    } with E { () =>
        def f() = { () }
        def g() = {
          resume(box f)
        }
        resume(box g)
    }
}
