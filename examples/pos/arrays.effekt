module examples/pos/arrays

import mutable/array

def main() = {
    val arr = emptyArray[String]();
    arr.put(0, "hello");
    arr.put(2, "world");
    println(arr.get(0));
    println(arr.get(2));
    println(arr.get(4));
    arr.put(4, "foo");
    println(arr.get(4))
}