module examples/pos/arrays

import mutable/array

def main() = {
    val arr = emptyArray[String]();
    put(arr, 0, "hello");
    put(arr, 2, "world");
    println(get(arr, 0));
    println(get(arr, 2));
    println(get(arr, 4));
    put(arr, 4, "foo");
    println(get(arr, 4))
}