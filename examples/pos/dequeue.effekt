module examples/pos/dequeue

import dequeue

def main() = {
    val dq = emptyQueue[Int]();
    inspect(dq.pushFront(1).pushFront(2));
    inspect(dq.pushFront(1).pushFront(2).popFront);
    inspect(dq.pushFront(1).pushFront(2).pushBack(3).popFront);
    inspect(dq.pushFront(1).pushFront(2).pushBack(3).popBack);
    inspect(dq.pushFront(1).pushFront(2).pushBack(3).first);
    inspect(dq.pushFront(1).pushFront(2).pushBack(3).last)
}