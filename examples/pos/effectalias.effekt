module effectalias

effect Pure = {}

interface Console {}

effect Foo = Console

effect Bar = { Foo, Foo }

def foo(): Unit / Foo = {
  println("hello")
}

def bar(): Unit / { Foo, Bar, Pure } = {
  println("hello")
}

def main(): Unit = try {
  foo()
} with Console {}