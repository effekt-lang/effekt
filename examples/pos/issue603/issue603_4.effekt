effect simple(): Unit

interface Bi {
  def foo(): Unit / { simple }
}

def test() = try {
  box new Bi {
    def foo() = do simple()
  }
  } with simple {
  resume(())
  box new Bi {
    def foo() = ()
  }
}
