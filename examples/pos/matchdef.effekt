module examples/pos/matchdef

import text/string

record Triple(first: Int, second: String, third: Boolean)

def main() = {
    val Triple(x, y, z) = Triple(1, "2", true);
    println(x + y.length);

    val (a, b) = (1, 2);
    println(a + b)
}