module examples/pos/overloading

import immutable/list
import immutable/option

def main() = {
    val l = [1,2,3];
    val o = Some(4);
    println(l.map { x => x + 1 });
    println(o.map { x => x + 1 })
}