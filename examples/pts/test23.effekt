effect Eff(): Unit

def run{x: _}: Int = {
    val a: Int = x(1)
    //def y: Int => Int / Eff = x
    1
}

def func(x: Int): Int / Eff = {
    do Eff()
    1
}

def main() = {
    run{ func }
    1
}