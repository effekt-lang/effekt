import dictionary
import stream
import bytearray

def main() = {
  val trie = dictionary::empty()

  trie.insert("a", "apple")
  trie.insert("ab", "amazing")
  trie.insert("b", "banana")

  for[Tagged[String]] { eachTagged(trie) } { case Tagged(key, value) =>
    println(bytearray::toString(key))
    println(value)
  }
}
