module examples/pos/exception/missing_value

import exception

def main() = {
  def stringOrMissing { p: => String / Exception[MissingValue] }: Unit = {
    with on[MissingValue].default { println("Value is missing") }
    println(p().show)
  }

  stringOrMissing { missingValue() }
  stringOrMissing { "Valid value" }
}
