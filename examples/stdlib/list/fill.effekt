module examples/pos/list/fill

import immutable/list

def main() = {
  val empty: List[Int] = fill(0, 42)
  println(empty)

  val single: List[Int] = fill(1, 42)
  println(single)

  val zeros: List[Int] = fill(5, 0)
  println(zeros)
}