module examples/pos/list/replace

import immutable/list

def main() = {
  // synchronized with doctest in `replace`
  println([1, 2, 3].replace(0, 42))
  println([1, 2, 3].replace(-1, 42))
  println([1, 2, 3].replace(10, 42))
}