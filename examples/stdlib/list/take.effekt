module examples/pos/list/take

import immutable/list

def main() = {
  // synchronized with doctests in `take`
  println([1, 2, 3].take(2))
  println([1, 2, 3].take(0))
  println([1, 2, 3].take(3))
  println([1, 2, 3].take(5))
  println([1, 2, 3].take(-1))
}