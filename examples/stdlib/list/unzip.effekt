module examples/pos/list/unzip

import immutable/list

def main() = {
  // synchronized with doctest in `unzip`
  val (lefts, rights) = [(1, 100), (2, 200), (3, 300)].unzip()
  println(lefts)
  println(rights)
}