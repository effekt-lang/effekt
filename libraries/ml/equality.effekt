module equality

import effekt

// Comparison ops
// ==============
extern pure def infixEq[R](x: R, y: R): Bool =
  ml "${x} = ${y}"

extern pure def infixNeq[R](x: R, y: R): Bool =
  ml "not (${x} = ${y})"