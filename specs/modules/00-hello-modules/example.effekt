module example

module hello {
    def modules(x: Int) = {
        println("Hello Modules")
    }

    def bar() = {
        modules(5)
    }
}

def main() = {
    hello:modules() // => Call
    //println(5)
}