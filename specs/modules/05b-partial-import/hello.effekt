module hello {
    def greet(name: String) = {
        println(concat("Hello ", name))
    }

    module world {
        def greet() = {
            greet("World")
        }

        def goodbye() = {
            println("Goodbye!")
        }

        def foo() = 4
    }
}